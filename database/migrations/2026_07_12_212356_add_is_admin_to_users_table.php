<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            // Gates the telemetry dashboard/devices/events routes (viewTelemetry Gate,
            // AppServiceProvider) — this User model doubles as the social API's end-user
            // principal, so without this every registered mobile-app user could otherwise
            // browse other users' devices and crash reports via the web dashboard.
            $table->boolean('is_admin')->default(false);
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->dropColumn('is_admin');
        });
    }
};
